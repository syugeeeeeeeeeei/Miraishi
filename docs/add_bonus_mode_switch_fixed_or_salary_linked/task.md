# タスク

## 依頼内容
ボーナスの仕様を拡張し、以下2モードを切り替えられるようにする。

1. 固定額モード（従来どおり）
2. 基本給連動モード（基本給の○ヶ月分）

## 背景
実務上、ボーナスは固定額ではなく基本給連動で扱われるケースが多いため、モード切替が必要。

## 完了条件
- UI上でモード切替できる。
- 基本給連動モード時に、各年の基本給に応じてボーナス額が変動する。
- 既存データ（`bonus` 未設定）でも固定額モードとして従来どおり動作する。
- `yarn typecheck` / `yarn test` / `yarn build` が通る。
