# 修正内容の確認

## 1. Main IPC の後方互換対応
- `get-tax-schema` と `update-tax-schema` の処理をそれぞれ共通ハンドラ関数へ切り出し。
- それらを以下チャンネル名に重複登録して互換性を確保。
  - 取得: `get-tax-schema`, `getTaxSchema`
  - 更新: `update-tax-schema`, `updateTaxSchema`

## 2. Renderer のローカルフォールバック
- `localStorage` に税金スキーマを保存/読込するヘルパーを追加。
- API取得成功時はローカルにも同期保存。
- API未登録時はローカル保存値を優先し、なければ内蔵デフォルトを使用してダイアログを開く。
- API更新未登録時もローカル保存 + 再計算で処理完了できるようにした。

## 3. トースト表示の改善
- 未登録時の文言を「ローカルモード」ベースに変更し、再起動前提の警告トーンを緩和。

## 4. 検証
- `yarn typecheck` 実施済み（エラーなし）。
