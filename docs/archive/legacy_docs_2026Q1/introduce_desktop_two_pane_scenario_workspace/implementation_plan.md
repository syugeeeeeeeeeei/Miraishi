# 実装計画

## 1. レイアウト刷新

- `ScenarioCard` 内の単一ビュー切替構成を廃止し、常時2ペイン構成へ変更。
- 左ペイン: 入力フォーム
- 右ペイン: 計算結果パネル

## 2. 余白最適化

- `ScenarioWorkspace` コンテナに `maxW` と中央寄せを適用し、超広画面での間延びを防止。
- タイトル入力に `maxW` を設け、横に伸びすぎないよう調整。

## 3. 検証

- `yarn typecheck`
- `yarn test`
- `yarn build`
