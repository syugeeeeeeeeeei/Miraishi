# 修正内容の確認

## 変更概要

右端アイコンの下部見切れを解消するため、円形メニュー全体を上に移動した。

## 変更点

- `SystemRadialMenu.tsx`
  - ルートコンテナの高さを `72px` -> `84px` に変更。
  - メニュー基準位置を `bottom={0}` -> `bottom="8px"` に変更。

## 検証結果

- `yarn typecheck` 成功
