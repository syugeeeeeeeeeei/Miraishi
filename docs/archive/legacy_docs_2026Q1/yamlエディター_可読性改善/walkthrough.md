# 修正内容の確認

## 1. エディター表示スタイルの改善
- YAML `Textarea` のフォントを日本語フォールバック付き等幅スタックへ変更。
- `fontSize` / `lineHeight` / `letterSpacing` を調整して可読性を向上。
- 背景に 2 文字幅ごとの縦ガイド線を追加し、インデント深さを追いやすくした。

## 2. YAML出力の構造化
- `toTaxSchemaYamlText` の出力にセクション見出しコメントを追加。
- トップレベル項目ごとに空行を挿入してブロック境界を明確化。
- `incomeTaxRates` の各要素に「N段目」コメントを追加。

## 3. 補助文言
- エディター下部の説明文に「半角スペース2つでインデントを揃える」案内を追加。

## 4. 検証
- `yarn typecheck:web`: 成功
- `yarn typecheck:node`: 成功
