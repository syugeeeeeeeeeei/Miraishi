# 修正内容の確認

## 変更ファイル

- `src/main/lib/scenarioComparisonPdf.ts`

## 主要変更

- シナリオサマリースライドから案内カード（年度詳細の説明カード）を削除。
- 初期入力条件の表示を拡大。
  - 見出しサイズを拡大
  - ラベル/値フォントサイズを拡大
  - カード内余白を拡大
- 年次推移グラフを縦長化。
  - SVGサイズを `760x380` に変更
  - プロット領域（余白）を再調整
  - グラフ表示領域の `min-height` を拡大

## 検証

- `yarn typecheck` 成功
- `yarn test --run` 成功（22 tests passed）
