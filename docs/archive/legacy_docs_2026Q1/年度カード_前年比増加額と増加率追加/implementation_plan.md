# 実装計画

1. 年度カード描画関数に「前年度データ」引数を追加する。
2. 額面・手取それぞれについて、前年度比の増加額と増加率を計算する。
3. 年度カード内に前年比表示領域（2項目）を追加する。
4. 年度詳細ページ描画時、同シナリオの全年度データから前年度を解決してカードへ渡す。
5. 1年目など前年度が無い場合は `-` 表示にする。
6. `yarn typecheck` と `yarn test --run` で回帰確認する。
