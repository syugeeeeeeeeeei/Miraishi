# 実装計画

1. 年次推移グラフ描画ロジックを拡張し、軸付きSVG（Y軸ラベル/X軸年次ラベル）を生成する。
2. 推移スライドのレイアウトを2列カードから縦積み大型カードへ変更する（1ページあたり最大2シナリオ）。
3. 各カードに分析サマリー文と主要KPIを追加し、比較の読み取りコストを下げる。
4. CSSを調整し、チャート領域の高さを確保してA4縦での表示密度を最適化する。
5. `yarn typecheck` と `yarn test --run` を実行して回帰確認する。
