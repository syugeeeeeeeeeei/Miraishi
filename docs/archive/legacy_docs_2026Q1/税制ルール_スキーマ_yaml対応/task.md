# タスク

## 依頼内容
- 税制ルール定義ファイルを JSON ではなく YAML で管理できるようにする。
- YAML のコメント機能を使って、スキーマに注釈を追加しやすくする。

## 完了条件
1. アプリが `resources/schema/tax_schema.yaml` を読み込んで税制ルールを初期化できる。
2. 既存の JSON 管理から YAML 管理へ移行しても起動時エラーが発生しない。
3. 関連ドキュメントの参照パスが YAML に更新されている。
4. 型チェック（少なくとも main 側）が通る。
