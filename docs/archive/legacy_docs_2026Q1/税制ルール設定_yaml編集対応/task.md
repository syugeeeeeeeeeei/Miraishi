# タスク

## 依頼内容
- アプリ内の税制ルール設定メニューを JSON 編集から YAML 編集へ変更する。
- YAML の利点を活かし、編集テキストにコメントを付けられるようにする。

## 完了条件
1. 税制ルールのエディットモードで YAML を入力・検証できる。
2. タブ名・ラベル・エラーメッセージが YAML 前提に置き換わっている。
3. 初期表示およびフォーム編集から反映されるテキストがコメント付き YAML になる。
4. 型チェックが通る。
