# 実装計画

## 1. 表示項目の整理

- `uiMeta.items` で確認画面に出す項目を制御しているため、`formula.steps` の定義を削除する。
- 対象は既定値生成（TypeScript）と配布スキーマ（YAML 2ファイル）の両方に揃える。

## 2. 文言修正

- 配列プレビュー文言を短く安定した日本語に変更する。
- 「関連づく」の不自然な表現を「関連する」に修正する。

## 3. 検証

- `yarn typecheck`
- `yarn test --run`
