# タスクリスト

- [x] 確認画面の項目一覧から `formula.steps` を除外
- [x] `tax_schema.yaml` の `uiMeta.items` から `formula.steps` を除外
- [x] `tax_schema_with_meta.yaml` の `uiMeta.items` から `formula.steps` を除外
- [x] 配列表示文言を `20件（配列）` 形式へ変更
- [x] 未関連文言を `関連する計算式は未設定です。` に修正
- [x] `yarn typecheck` 実行
- [x] `yarn test --run` 実行
