# タスク

ボーナス設定の計算反映タイミングを改善し、ユーザー操作に対して即時に結果へ反映されるようにする。

## 要件

- ボーナスの「固定額 / 基本給連動」切り替えボタンを押した時点で再計算する。
- 切り替え先の入力フォーム（固定額入力 or 支給月数入力）で値を編集し、フォーカスが外れた時点で再計算する。

## 目的

- 操作と結果反映のタイムラグを減らし、設定変更の手応えを明確にする。
