# タスク

## 依頼内容
給与成長率の計算挙動を見直し、`100%` 設定時に期待どおりの増加が反映されない問題を修正する。

## 観測された症状
- 給与成長率を高く設定しても、結果表示が微増のままに見える。

## 完了条件
- 保存後の再計算で最新のシナリオ値が確実に使われる。
- 給与成長率100%で基本給が年次で倍増することをテストで担保する。
- `yarn typecheck` / `yarn test` / `yarn build` が通る。
