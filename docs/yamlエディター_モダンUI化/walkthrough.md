# 修正内容の確認

## 1. モダンなエディターコンテナ
- YAML エディターをカード化し、角丸・境界線・シャドウでモダンな見た目に変更。
- 上部にツールバーを追加し、編集状態を一目で確認できる UI にした。

## 2. ツールバー/ステータス表示
- バッジで `YAML` / `2 spaces` / `構文OK or 構文エラー` を表示。
- 右上に補助情報（line numbers / fold / bracket match）を表示。

## 3. フォーマット操作の追加
- `フォーマット` ボタンを追加。
- 現在の `draftTaxSchema` からコメント付き YAML を再生成し、崩れたレイアウトを戻せるようにした。

## 4. テーマの再設計
- ガター色、アクティブ行、選択色、カーソル色を調整。
- トークン色（コメント・文字列・数値・キー）を最適化し、読みやすさを改善。
- 空白可視化（青点/青矢印/赤帯）は維持。

## 5. 検証
- `yarn typecheck:web`: 成功
- `yarn typecheck:node`: 成功
