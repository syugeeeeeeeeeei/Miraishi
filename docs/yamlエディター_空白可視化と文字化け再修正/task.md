# タスク

## 依頼内容
- WSL GUI 環境で YAML エディターの日本語等幅フォントが文字化けする問題を修正する。
- YAML エディター内で空白スペースやタブを可視化し、インデントを追いやすくする。

## 完了条件
1. WSL GUI でも文字化けしにくいフォント構成になっている。
2. スペースとタブ、行末空白がエディター上で視認できる。
3. 既存の YAML 編集・検証・保存フローが維持される。
4. 型チェックが通る。
