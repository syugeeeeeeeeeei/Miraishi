# 修正内容の確認

## 1. Apple風マーク削除
- YAMLエディターヘッダーの赤・黄・緑マークを削除。
- ヘッダーは `YAML EDITOR` と構文ステータスのみのミニマル構成にした。

## 2. 日本語フォントの安定化
- フォントスタックを `var(--chakra-fonts-body)` 優先へ変更。
- アプリ既定フォントを優先することで、WSL GUI 環境での日本語崩れを抑える構成にした。

## 3. スペース可視化の薄色化
- `.cm-highlightSpace` を薄いグレー系ドットへ変更。
- ドットサイズも小さくし、画像イメージに近い控えめな表示に調整。
- あわせてタブ矢印色も低彩度化して主張を抑えた。

## 4. 検証
- `yarn typecheck:web`: 成功
- `yarn typecheck:node`: 成功
