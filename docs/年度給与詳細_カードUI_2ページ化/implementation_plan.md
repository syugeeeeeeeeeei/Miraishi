# 実装計画

1. 年度詳細テーブル生成ロジックを撤去し、年度カード生成ロジックへ置換する。
2. 税金内訳はカード内に色棒グラフ＋凡例として表示する。
3. 年度データを2分割し、各シナリオにつき年度詳細を2ページ固定で出力する。
4. カードUI用CSS（ヘッダー、主要値、月給値、税内訳グラフ）を追加する。
5. `yarn typecheck` と `yarn test --run` で回帰確認する。
