# 実装計画

1. PDF生成ロジックを「比較セクション型」から「シナリオ中心サマリー型」へ切り替える。
2. 各シナリオについて、初期入力条件・年次推移グラフ・年度別給与詳細テーブルを1セットで出力する。
3. 年度別テーブルの列を要件に合わせる（年収額面/年収手取/税金の内訳/月給額面/月給手取り）。
4. 年数が多い場合はテーブルをページ分割し、続きを自動生成する。
5. 最後に税制スキーマと計算前提を出力する。
6. 型チェックとテストを実行して回帰確認する。
