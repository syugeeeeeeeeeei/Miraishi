# 修正内容の確認

## 変更ファイル

- `src/main/lib/scenarioComparisonPdf.ts`

## 主要変更

- レポート構成を再編し、以下の順序に統一。
  - 表紙
  - 各シナリオサマリー（シナリオ単位で連続出力）
  - 税制スキーマと計算前提
- 各シナリオサマリーに以下を統合。
  - 初期入力条件
  - 年次推移グラフ
  - 年度ごとの給与詳細テーブル
- 年度ごとの給与詳細テーブル列を要件どおり実装。
  - 年収額面
  - 年収手取
  - 税金の内訳（所得税/復興税/住民税）
  - 月給額面
  - 月給手取り
- 年数が多いケースに対応し、テーブルを続きページへ自動分割。
- A4縦のページ領域を活かすレイアウトへ調整。

## 検証

- `yarn typecheck` 成功
- `yarn test --run` 成功（22 tests passed）
